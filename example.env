# Data-MultiVector Environment Variables

# Flask configuration
SECRET_KEY=your_secret_key   # Use a secure and random string as the secret key
FLASK_ENV=development       # Set to 'development' for development mode; 'production' for production mode

# MySQL Database configuration
DB_HOST=localhost          # Database host address
DB_PORT=3306               # Database port number
DB_USER=your_db_user       # Database username
DB_PASSWORD=your_db_password   # Database password
DB_NAME=your_db_name       # Database name

# Configuration for input paths (comma-separated)
INPUT_PATHS=/path/to/input/directory1,/path/to/input/directory2,/path/to/input/directory3,/path/to/input/directory4,/path/to/input/directory5
# Configuration for output paths (comma-separated)
OUTPUT_PATHS=/path/to/output/directory1,/path/to/output/directory2,/path/to/output/directory3,/path/to/output/directory4,/path/to/output/directory5

# Other configuration
# You can add more input and output paths by extending the comma-separated lists.
# For example:
# INPUT_PATHS=/path/to/input/directory1,/path/to/input/directory2,/path/to/input/directory3
# OUTPUT_PATHS=/path/to/output/directory1,/path/to/output/directory2,/path/to/output/directory3
